{% extends "layout.html.twig" %}

{% block title %} Fiche auteur {% endblock %}

{% block content %}
    <div class="row d-flex justify-content-center text-center">

    <div>
        <a href=" {{ path('app_auteur', {id: auteur.id}) }} "><i class="fas fa-edit mx-1 text-success"></i></a>
        <a href="" onClick="return confirm('Êtes-vous sur de vouloir supprimer l\'auteur ?')"><i class="fas fa-trash mx-1 text-danger"></i></a>
    </div>

        <h1> {{ auteur.prenom ~ ' ' ~ auteur.nom }} </h1>
        <h6 class="text-muted"> Date de naissance : {{ auteur.dateDeNaissance|date("d/m/Y") }} </h6>
        
        <h6>Biographie :  </h6>
        <p> {{ auteur.biographie }} </p>

        <div>
            <a href=" {{ path('app_auteurs') }} " class="btn btn-primary card-link">Retour</a>
        </div>
    </div>
{% endblock %}